#include <iostream>
using namespace std;
 
#define N 100
 
int main() {
    int n, r, c;
    cin >> n;
    int arr[N][N];
    for (int i = 0; i < n; i++) {
        for (int j = 0 ; j < n; j++) 
            cin >> arr[i][j];
    }
    cin >> r >> c;
    for (int i = 0; i < r; i++) {
        for (int j = 0; j < c; j++) {
            cout << arr[i][j] << " ";
        } cout << endl;
    }
    return 0; 
}
